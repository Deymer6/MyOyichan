<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para API OpenFDA y conexión de red -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permisos para Asistente de Voz -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Permisos para Llamadas SOS -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <!-- Permisos para Recordatorios/Alarmas (API >= 31) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Permisos para Notificaciones (API >= 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyOyichan"
        android:usesCleartextTraffic="true">
        
        <!-- Auth Activities -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />
            
        <activity
            android:name=".ui.auth.RecoverPasswordActivity"
            android:exported="false" />

        <!-- Main Flow -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />
            
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false" />
            
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <!-- Formularios -->
        <activity
            android:name=".ui.medicamentos.MedicamentoFormActivity"
            android:exported="false" />
            
        <activity
            android:name=".ui.medicamentos.InfoMedicamentoActivity"
            android:exported="false" />
            
        <activity
            android:name=".ui.vitales.VitalFormActivity"
            android:exported="false" />
            
        <activity
            android:name=".ui.contactos.ContactoFormActivity"
            android:exported="false" />

        <!-- Receivers -->
        <receiver
            android:name=".utils.AlarmReceiver"
            android:exported="false" />
            
    </application>

</manifest>